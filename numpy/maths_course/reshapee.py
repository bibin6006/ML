import numpy as np
arr=np.array([1,2,3,4,5,6])
multi_array=np.reshape(arr,(3,2))
print(multi_array)

print(multi_array.ndim)
print(multi_array.shape)
print(multi_array.size)# return the number of elements
